\documentclass[sigconf,anonymous]{acmart}

\settopmatter{printacmref=false}
\renewcommand\footnotetextcopyrightpermission[1]{}
\acmConference{ASP-DAC}{2027}{}
\acmYear{2027}
\acmISBN{}
\acmDOI{}

% --- GA-on-softmax results: filled in once the convergence run completes.
% Placeholder values below render visibly so an unfilled draft is obvious.
\newcommand{\GAred}{\textbf{[GA\%]}}
\newcommand{\GAcalls}{\textbf{[GA\,calls]}}
\newcommand{\GAhours}{\textbf{[GA\,h]}}

\title{Amortized Custom eFPGA Layout Search via Reinforcement Learning: Zero-Shot Generalization Across Circuit Benchmarks}

\begin{document}

\begin{abstract}
Embedding a small reconfigurable fabric into an ASIC to implement one
security-critical IP block is a known defense against reverse engineering,
since the protected function is absent from the fixed silicon until
configured. Because this use case targets one known netlist rather than
general-purpose reconfigurability, a custom-tailored fabric layout can
recover much of the area/delay/power overhead a generic FPGA would
otherwise impose. Prior work (GOLDS and a follow-up NSGA-II method)
searches for such layouts with an evolutionary algorithm re-initialized
from scratch for every new netlist. We instead train a
reinforcement-learning policy, conditioned on a graph-neural-network
encoding of the target netlist, to choose DSP/BRAM placement and fabric
aspect ratio, evaluated directly against the VTR synthesis-to-routing
flow. Under a controlled comparison against our own
genetic algorithm with an identical oracle and metric on a held-out benchmark,
the policy reaches a comparable ADP reduction while the GA must re-search the
layout from scratch --- a single zero-shot policy rollout lands in the same
region the GA reaches only after hundreds of VTR evaluations and hours of
wall-clock. Jointly
trained across 11 circuits, it reaches a 27.35\% aggregate
in-pool ADP reduction averaged over three training seeds. Recomputed on GOLDS' published metric and
baseline, a zero-shot evaluation with no training on the
target benchmark reaches 37.4\% against GOLDS' reported
$\sim$35\%. Across the three seeds we further characterize result
reliability and find a marked asymmetry: in-pool performance is
substantially more seed-sensitive than zero-shot generalization, with two
in-pool benchmarks spanning $\sim$28 points across seeds while
every held-out benchmark spans at most $\sim$12. We report the individual
per-seed results rather than a mean $\pm$ SD that three runs cannot justify.
\end{abstract}

\maketitle

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{fig_scale.pdf}
\caption{Zero-shot scale extrapolation. The policy is trained only on small
fabrics ($\le$676 grid tiles; shaded band). With a single zero-shot rollout per
benchmark it produces ADP reductions on all six held-out benchmarks. The four
larger ones lie to the \emph{right} of the training band --- out of
distribution in size, up to $2{,}304$ tiles ($\sim$$3.4\times$ the area and
$\sim$$5\times$ the logic of anything in the training pool) --- and every one
is positive across all three seeds. The two small benchmarks
(\texttt{custom\_macbuf}, \texttt{lightweight\_cipher}; grey) are in-size
controls. Dots are three seeds (7/42/123), the tick is the median, and the line
spans min--max. All reductions are against the same traditional baseline.}
\label{fig:scale}
\end{figure}

\section{Introduction}
\label{sec:intro}

Embedding a small, reconfigurable fabric into an ASIC to implement one
security-critical IP block — so that the function is not recoverable from the
silicon layout or GDSII without the bitstream — is a known defense against
reverse engineering and IP theft~\cite{golds}. A generic, general-purpose
FPGA architecture is built to serve arbitrary circuits, and is therefore
over-provisioned — extra LUTs, routing, and DSP/BRAM capacity — for any one
fixed design that will only ever run a single bitstream. Because the eFPGA
masking use case targets exactly one known application on a small embedded
fabric, rather than general-purpose reconfigurability, a custom-tailored
architecture and placement, sized and arranged specifically for that one
netlist, is viable, and can recover much of the area/delay/power overhead a
generic FPGA would otherwise impose. We use the product of area, delay, and
power (ADP) as our optimization target throughout this paper because it is
precisely the size of this overhead — the ``masking tax'' of moving a
protected block onto reconfigurable fabric instead of hardening it directly
into standard cells.

Two prior papers establish that per-instance search over DSP/BRAM placement
(and, in one case, CLB/arithmetic-block sizing) can substantially reduce this
tax: GOLDS~\cite{golds} uses a genetic algorithm with an Area$\times$Delay
fitness function and reports up to $\sim$35\% ADP improvement on the
\texttt{diffeq1} benchmark; a follow-up applies NSGA-II to a larger,
multi-objective search space that additionally co-evolves CLB LUT size and
custom arithmetic-block sizing~\cite{nsgaii}. Both, however, are \emph{testbench-specific}: the genetic algorithm's
population is re-initialized and re-evolved from scratch for every new
protected IP block, and every generation of every run requires its own full,
expensive VTR evaluation. Nothing learned while optimizing one netlist's
layout carries over to the next netlist — each new target benchmark pays the
full search cost again, with no way to amortize the work already done on
previous designs.

This paper asks whether a \emph{learned policy} can replace that
per-instance search loop. RL placing blocks onto an \emph{already-fixed} FPGA
fabric is not new; we pose two questions instead. \textbf{RQ1}: can the same
RL machinery \emph{construct} the custom fabric layout itself — DSP/BRAM
placement and aspect ratio for an embedded FPGA built around one netlist — the
regime where a per-instance GA has so far been the only option? \textbf{RQ2}:
can such a policy be \emph{benchmark-agnostic}, pretrained across a pool of
netlists so the same checkpoint speeds up layout for a previously unseen
benchmark (zero-shot or after brief fine-tuning) rather than re-searching from
scratch?

We train a single reinforcement-learning policy, conditioned on a
graph-neural-network encoding of the target netlist, to place DSP/BRAM hard
blocks and choose a fabric aspect ratio, evaluated directly against the real
VTR (Yosys + VPR) synthesis-to-routing flow rather than a learned or analytic
proxy. Because the same checkpoint applies to a netlist it has never seen
without re-running search, the cost of finding a good layout is amortized
across deployments. The objective is therefore not to win every
single-instance ADP comparison outright, but to produce a strong layout for
a previously unseen netlist at near-zero search cost --- even one several
times larger than any fabric seen in training (Figure~\ref{fig:scale}).

The paper sits at the intersection of two lines of work not previously
connected (Section~\ref{sec:related}): custom eFPGA layout search for logic
masking, which to date has used only per-instance evolutionary
search~\cite{golds,nsgaii}, and learned policies for physical design, which
have targeted ASIC macro placement at a much larger scale and through a
learned reward proxy rather than the true toolchain~\cite{alphachip,chipformer}.
We bring the amortization argument from the latter to the former problem,
evaluated end-to-end against the same VTR oracle the former already
established as the fidelity bar.

Concretely, this paper contributes:
\begin{itemize}
  \item A reinforcement-learning formulation of custom eFPGA DSP/BRAM
        placement and aspect-ratio selection, with a GCN-based netlist
        encoding (Section~\ref{sec:gnn}) and a fixed-size policy that remains
        loadable against benchmarks never seen during training
        (Section~\ref{sec:universe}), evaluated directly against the same
        VTR-based oracle used by prior eFPGA-layout search work.
  \item A controlled cost comparison against a from-scratch genetic algorithm
        under an identical evaluation oracle, ADP metric, and baseline, on a
        held-out benchmark: the policy reaches the GA's ADP reduction in a
        single zero-shot oracle call, against the hundreds of calls and hours
        of search the GA re-pays for every new netlist
        (Section~\ref{sec:results-ga}).
  \item A joint 11-benchmark training result (27.35\% aggregate in-pool ADP
        reduction, averaged over three seeds) (Section~\ref{sec:results-inpool}),
        and a literature-facing comparison against GOLDS' published metric and
        baseline, recomputed on a zero-shot evaluation: 37.4\% against GOLDS'
        $\sim$35\% (Section~\ref{sec:results-golds}).
  \item A three-seed reliability analysis exposing an asymmetry: in-pool
        fitting is far more seed-sensitive (two benchmarks spanning
        $\sim$28 points across seeds) than zero-shot generalization
        (at most $\sim$12 points across all six held-out benchmarks),
        reported as raw per-seed results rather than mean $\pm$ SD
        (Sections~\ref{sec:results-zeroshot},~\ref{sec:results-tradeoff}).
\end{itemize}

\section{Background and Related Work}
\label{sec:related}

\subsection{eFPGA-Based Logic Masking}

Embedding a reconfigurable fabric into an ASIC to hide one IP block's
function behind a bitstream is a hardware-security technique distinct from
standard logic locking or camouflaging, since the protected function is not
merely obfuscated in the netlist but physically absent from the fixed
silicon until configured. Automated flows for partitioning RTL into embedded
reconfigurable fabrics established eFPGA redaction as a practical
defense~\cite{alice,arianna}, and subsequent work has cut its overhead by
shrinking under-utilized routing~\cite{shell} or de-regularizing the fabric
itself~\cite{nuredact} — motivating the custom, non-island-style layouts this
paper synthesizes. These flows all rely on a non-learning search (heuristic or
evolutionary) re-run per design; none amortizes layout effort across netlists. GOLDS~\cite{golds} introduced custom-tailored
eFPGA layout design for this purpose: a genetic algorithm searches over
DSP/BRAM tile placement on a fixed-size fabric, with an
Area$\times$Delay fitness function evaluated through the real VTR flow, and
reports up to $\sim$35\% ADP-relevant improvement over a traditional
island-style layout on \texttt{diffeq1}. A follow-up~\cite{nsgaii} extends
this to a multi-objective NSGA-II search that additionally co-evolves CLB
LUT size and custom DSP/BRAM block sizing — not just their placement —
optimizing delay and power jointly and reporting up to $\sim$44.6\%
improvement on a larger design-space. Both papers' searches are run fresh,
from a random population, for every new protected design; neither carries
information learned on one IP block forward to the next. This paper targets
the same problem GOLDS defines (custom DSP/BRAM placement plus aspect ratio,
evaluated through the same VTR-based oracle) and treats it as the
narrower, more directly comparable target for our controlled experiments
(Section~\ref{sec:results-ga}), while citing the NSGA-II extension as
context for the larger design space a future version of this approach could
also cover (Section~\ref{sec:conclusion}).

\subsection{Learned Policies for Physical Design}

Reinforcement learning has been applied to chip-scale physical design.
AlphaChip~\cite{alphachip,alphachip_addendum} trains an RL agent to place ASIC
macro blocks using a learned reward proxy, and reports policies that transfer
across chip designs without retraining — the amortization argument we make
here, at a much larger design scale. Subsequent work explores visual state
representations~\cite{maskplace}, joint placement-and-routing~\cite{deeppr},
and offline RL over a transformer policy~\cite{chipformer}. All motivate the
claim this paper tests in a smaller-scale setting: a trained policy can
out-amortize per-instance search once many designs must be placed. Unlike
AlphaChip, we evaluate every candidate against the true VTR flow rather than a
learned proxy, trading speed for fidelity — affordable at our scale, where a
VTR run completes in seconds to low minutes (Section~\ref{sec:oracle}).

Within the FPGA setting specifically, RL has guided placement onto an
\emph{already-fabricated} grid — selecting annealing perturbations in
VPR~\cite{rlplace} or driving a Gym-style VPR environment~\cite{vprgym} — but
always optimizes the assignment of blocks to a fixed grid, never its geometry.
Transferable EDA policies have likewise appeared for tool tuning~\cite{fasttuner}
and global placement~\cite{transplace}, but not, to our knowledge, for
\emph{synthesizing} a custom FPGA fabric itself, which is what our policy does.

\subsection{VTR/VPR Background}

We build on the open-source Verilog-to-Routing (VTR)~\cite{vtr9,vtr8,vpr97}
toolchain: Yosys performs synthesis (via the VTR integration~\cite{parmys})
and VPR performs packing, placement, and
routing against an architecture described in an XML schema of tile types,
positions, and connectivity. This architecture XML is the artifact GOLDS and
the NSGA-II follow-up generate by hand-coded search and that we generate by
policy rollout (Section~\ref{sec:oracle}); VTR's acceptance of an arbitrary
heterogeneous tile layout, not only the regular island-style grids it ships,
is what makes this entire line of work possible.

\section{Methodology}

\subsection{Problem Formulation}
\label{sec:problem}

We separate two roles that are easily conflated. The \emph{policy} designs the
fabric; the \emph{VTR toolchain} maps the circuit onto it. Concretely, given a
netlist requiring $n_{\mathrm{dsp}}$ DSP and $n_{\mathrm{bram}}$ BRAM hard
blocks, the policy emits an architecture descriptor
$\pi = (\mathrm{ar}, \{(x_i,y_i)\}_{i=1}^{n_{\mathrm{dsp}}+n_{\mathrm{bram}}})$:
a fabric aspect ratio $\mathrm{ar} \in \mathcal{R}$ and a tile coordinate for
every hard block. VTR then runs the full synthesis-to-routing flow on that
architecture --- it synthesizes the Verilog, packs the remaining logic into CLB
clusters, places every block (CLBs included), routes, and auto-sizes the array
to the smallest grid of the chosen aspect ratio that holds the design. The
policy thus never places a CLB or fixes the grid dimensions: it shapes the
fabric and positions the hard blocks, and VTR reports back the realized cost.
We take that cost to be the product of routing area, critical-path delay, and
power (ADP), since it directly reflects the silicon and timing overhead of
moving the protected IP block onto reconfigurable fabric:
\begin{equation}
\label{eq:adp}
  \mathrm{ADP}(\pi) = \mathrm{Area}(\pi) \times \mathrm{Delay}(\pi) \times \mathrm{Power}(\pi),
\end{equation}
where each term comes from the VTR (Yosys + VPR) flow (Section~\ref{sec:setup}).
We report results as percentage ADP reduction relative to a traditional,
non-customized island-style eFPGA baseline at the same logic capacity.

\subsection{RL Formulation}
\label{sec:rl}

We pose placement as a finite-horizon sequential decision process and train a
policy with Proximal Policy Optimization (PPO), using the maskable
variant~\cite{maskableppo} (\texttt{sb3\_contrib}'s \texttt{MaskablePPO}) so
that the action space can be
restricted to currently-legal tile coordinates at every step.

\textbf{Episode structure.} An episode lays out exactly one benchmark's hard
blocks on the shared canvas. Step 0 selects the fabric aspect ratio from a fixed
discrete set $\mathcal{R}$; each subsequent step positions one hard block ---
BRAMs first, then DSPs, each group ordered by descending shared-net
connectivity, so the most-connected blocks are committed earliest. Once all
hard blocks are positioned, the environment bakes the layout into an
architecture XML and a placement-constraints file (via Jinja2 templating) and
invokes the VTR flow to obtain $\mathrm{Area}(\pi)$, $\mathrm{Delay}(\pi)$, and
$\mathrm{Power}(\pi)$; all intermediate steps receive zero reward.

\textbf{Action space.} The action space is $\mathrm{Discrete}(W_{\max} \times
H_{\max})$, fixed across all benchmarks in a training run (Section~\ref{sec:universe}).
At step 0 an action index selects an entry in $\mathcal{R}$; at later steps an
action index decodes to a tile coordinate $(x,y)$ via a fixed stride on
$H_{\max}$, independent of which benchmark is active. Action masking — keyed
off the \emph{active} benchmark's real width/height and the blocks already
placed — restricts the policy to legal, non-overlapping, in-bounds coordinates
for that specific benchmark and step.

\textbf{Reward.} The terminal reward is a weighted sum of negative log-ratios
against the traditional baseline for the active benchmark:
\begin{equation}
\label{eq:reward}
  r = -\Big(
    w_a \log\tfrac{\mathrm{Area}}{\mathrm{Area}_0} +
    w_d \log\tfrac{\mathrm{Delay}}{\mathrm{Delay}_0} +
    w_p \log\tfrac{\mathrm{Power}}{\mathrm{Power}_0}
  \Big),
\end{equation}
where $\mathrm{Area}_0,\mathrm{Delay}_0,\mathrm{Power}_0$ are the
traditional-baseline metrics for that benchmark and $w_a,w_d,w_p$ are
configurable weights (Section~\ref{sec:training}). With $w_a=w_d=w_p=1$,
$r$ equals $\log(\mathrm{ADP}_0/\mathrm{ADP})$ — i.e.\ the reward
\emph{is} the log of the ADP-improvement ratio in Eq.~\ref{eq:adp}, making
reward and our headline metric directly comparable. An invalid choice
(degenerate aspect ratio, out-of-bounds or overlapping placement, or a VTR
flow failure) terminates the episode early with a fixed penalty reward of $-10$.

\subsection{GCN Feature Extractor}
\label{sec:gnn}

To let a single policy generalize across benchmarks with different netlist
sizes, hard-block counts, and grid dimensions, we encode each netlist as a
graph rather than a flat vector, giving the policy the inductive bias to
transfer across circuit topologies that graph learning provides
elsewhere in EDA~\cite{gamora}. We first reduce each benchmark's full netlist
graph to a compact, fixed-schema representation: one
node per DSP/BRAM hard-block instance, plus one aggregate ``fabric'' node
summarizing the remaining standard-cell logic. Each node carries a 4-dimensional
feature vector \texttt{[is\_dsp, is\_bram, is\_fabric, net\_count\_normalized]}.
Edges between two hard-block nodes are weighted by their raw shared-net count;
edges between a hard-block node and the fabric node are weighted by the summed
shared-net count across the standard cells that block touches. As these two
edge kinds live on very different scales, we normalize each into $[0,1]$
separately by a log compression $w_{\mathrm{norm}} = \log(1+w)/\log(1+c)$ with
a per-kind ceiling $c$ from the training pool, so neither type saturates or
vanishes.

This reduced graph is padded to a fixed $(\mathrm{MAX\_NODES},
\mathrm{MAX\_EDGES})$ — computed once over the full benchmark \emph{universe}
(Section~\ref{sec:universe}) — and fed to a small GNN feature extractor shared
by the policy and value heads: two \texttt{GCNConv} layers (hidden width 64)
produce per-node embeddings $h_i$; a global mean pool gives a whole-netlist
embedding, and the embedding of the node being placed this step gives a
focused per-step embedding. These are concatenated with a small CNN over the
occupancy grid (4 channels: occupancy, block-type hint, aspect ratio, net
count) and the active benchmark's normalized $(\mathrm{width},
\mathrm{height})$, then projected to a 128-dimensional vector for the PPO
policy and value heads. The extractor is trained end-to-end under the PPO
loss, with no separate GNN pretraining.

\subsection{Universe Superset and Action Masking}
\label{sec:universe}

A policy's input/output dimensions are fixed at construction, yet we want one
checkpoint loadable against benchmarks never trained on, including ones
discovered later. We therefore size every dimension-dependent quantity —
canvas $(W_{\max}, H_{\max})$, $\mathrm{MAX\_NODES}$, $\mathrm{MAX\_EDGES}$ —
from a benchmark \emph{universe} that is a deliberate superset of the sampled
training set, including benchmarks reserved purely for zero-shot evaluation,
provided none exceeds the shared dimensions. Per-episode action masking
(Section~\ref{sec:rl}) and the \texttt{valid\_wh} observation let the same
fixed-size network operate on each benchmark's smaller real footprint within
that canvas.

\subsection{Training Setup}
\label{sec:training}

Each training run samples uniformly across the 11-benchmark training pool
(Table~\ref{tab:benchmarks}) episode-by-episode, so a single policy is
updated jointly on all 11 circuits rather than one at a time. We use
\texttt{MaskablePPO} with 24 parallel environments, 48 rollout steps per
environment per update, batch size 144, 15 epochs per update, entropy
coefficient 0.01, and reward weights $w_a=w_d=w_p=1$ (Eq.~\ref{eq:reward}),
giving reward $=\log(\mathrm{ADP}_0/\mathrm{ADP})$. Each run trains for
13{,}000 episodes (capped independently of total timesteps) with a
per-episode VTR timeout of 300s to keep a degenerate early-exploration
episode from stalling the other 23 parallel workers. We report results from
three independent training seeds (7, 42, 123) under this identical
configuration, used throughout Section~\ref{sec:results} to characterize
result reliability rather than report a single run's numbers as ground
truth.

\section{Experimental Setup}
\label{sec:setup}

\subsection{Evaluation Oracle}
\label{sec:oracle}

Every candidate placement is scored by the true downstream toolchain, not a
proxy. A placement (aspect ratio + hard-block coordinates) is rendered into a
complete VTR architecture XML and a VPR placement-constraints file via Jinja2
templates parameterized by tile coordinates, block type, and fabric aspect
ratio. This architecture and the benchmark's Verilog are then run through the
full VTR flow — Yosys-based synthesis, VPR packing, placement, and routing,
followed by VPR's power estimator against a 45nm PTM technology file — and we
extract routing area, critical-path delay, and dynamic power from
the resulting VPR reports. All evaluations use a VTR 9.0.0 development build
(git \texttt{22a09d39}, GCC 13.3.0). Evaluated (placement, benchmark) pairs
are cached, since the synthesis-to-routing flow is the dominant cost of both
training and evaluation (seconds to low minutes per call depending on
benchmark size).

\subsection{Baseline}
\label{sec:baseline}

Our traditional baseline is VTR's default island-style architecture template
(\texttt{k6\_frac\_N10\_mem32K\_40nm}: 6-input LUTs, cluster size 10, 32K-bit
BRAMs, 40nm) at matched logic capacity for each benchmark — the same template
used by GOLDS~\cite{golds} as its traditional-layout comparison point, which
lets us compare percentage improvements directly against theirs on a shared
starting point (Section~\ref{sec:results-golds}).

\subsection{Benchmarks}
\label{sec:benchmarks}

Table~\ref{tab:benchmarks} lists the benchmark pool used in this work: an
11-benchmark training set of real circuits spanning a range of DSP/BRAM
resource profiles, and a held-out set of six circuits used only for zero-shot
evaluation (Section~\ref{sec:results-zeroshot}), never sampled during training.
Training circuits come from the standard VTR suite~\cite{vtr8}; three held-out
benchmarks (\texttt{softmax}, \texttt{reduction\_layer}, \texttt{arm\_core})
together with \texttt{mkDelayWorker32B} are larger standard circuits (from
Koios~\cite{koios} and the VTR suite) that also probe fabric sizes well beyond
the training pool. Because the standard suites contain few \emph{small}
heterogeneous designs, we add two small probes —
\texttt{lightweight\_cipher} and \texttt{custom\_macbuf}, the latter an
internally built multiply-accumulate buffer — to retain coverage at the
small-fabric end of the held-out set. The GA head-to-head
(Section~\ref{sec:results-ga}) uses \texttt{softmax}, a standard Koios
benchmark held out of RL training.

\begin{table}[t]
\caption{Benchmark pool: grid size and hard-block requirements.}
\label{tab:benchmarks}
\begin{tabular}{@{}lrrr@{}}
\toprule
Benchmark & Grid & DSP & BRAM \\
\midrule
\multicolumn{4}{@{}l}{\textit{Training set (11 benchmarks)}} \\
fifo               & 6$\times$6   & 0 & 1  \\
ch\_intrinsics     & 10$\times$10 & 0 & 1  \\
diffeq2            & 14$\times$14 & 5 & 0  \\
boundtop           & 13$\times$13 & 0 & 1  \\
diffeq1            & 14$\times$14 & 5 & 0  \\
spree              & 12$\times$12 & 1 & 3  \\
mkSMAdapter4B      & 18$\times$18 & 0 & 5  \\
or1200             & 25$\times$25 & 1 & 2  \\
mmc\_core          & 13$\times$13 & 0 & 1  \\
mkPktMerge         & 26$\times$26 & 0 & 15 \\
raygentop          & 17$\times$17 & 6 & 1  \\
\midrule
\multicolumn{4}{@{}l}{\textit{Held-out (zero-shot only)}} \\
softmax            & 41$\times$41 & 8 & 0  \\
reduction\_layer   & 42$\times$42 & 0 & 32 \\
lightweight\_cipher& 12$\times$12 & 0 & 3  \\
custom\_macbuf      & 12$\times$12 & 3 & 1  \\
mkDelayWorker32B   & 48$\times$48 & 0 & 43 \\
arm\_core          & 35$\times$35 & 0 & 24 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Baselines Compared}
\label{sec:baselines-compared}

We compare against: (1) the traditional island-style eFPGA baseline
(Section~\ref{sec:baseline}); (2) a genetic algorithm we implemented under the
same evaluation oracle and ADP metric (Section~\ref{sec:results-ga}); and
(3) published results from GOLDS~\cite{golds} and the NSGA-II
follow-up~\cite{nsgaii}, subject to the metric caveat in
Section~\ref{sec:results-golds}.


\section{Results}
\label{sec:results}

\subsection{In-Pool Performance}
\label{sec:results-inpool}

\begin{figure}[t]
\centering
\includegraphics[width=0.86\columnwidth]{fig_seed_variance.pdf}
\caption{Per-seed ADP reduction for every benchmark; this figure carries the
per-benchmark results in place of a table. Each benchmark shows its three
training-seed outcomes (dots; seeds 7/42/123), the median (tick), and the
min--max range (line) --- raw seeds rather than mean $\pm$ SD, which three
runs cannot justify. In-pool fitting is seed-sensitive on two benchmarks,
\texttt{or1200} and \texttt{boundtop} (shaded), whose ranges span
$\sim$28 and $\sim$29 points; every other in-pool benchmark and all six
held-out (zero-shot) benchmarks are tight (range $\le12$ points). The held-out
group is more seed-stable than in-pool fitting.}
\label{fig:seedvar}
\end{figure}

Figure~\ref{fig:seedvar} (upper group) reports per-benchmark in-pool ADP
reduction for the 11-benchmark policy on its own training set across three
independent seeds (7, 42, 123; Section~\ref{sec:training}). We show all three
per-seed outcomes rather than a mean $\pm$ SD, which three runs cannot
justify (Section~\ref{sec:results-tradeoff}). Each seed's pooled aggregate
— one minus the ratio of summed ADP across all 11 benchmarks to summed
baseline ADP, \emph{not} a simple mean of per-benchmark percentages — is
35.73\%, 23.11\%, and 23.97\% (mean 27.35\%).
\texttt{raygentop} is a persistent laggard, every seed within $[-2.2, -1.7]\%$
and never positive. Its 6-DSP/1-BRAM profile is the only one in the pool
mixing a large DSP count with a BRAM (other DSP-heavy benchmarks have no BRAM;
other BRAM-having ones have $\le1$ DSP), and despite a fair $\approx$9.1\%
episode share the policy never solves this joint placement — plausibly a
representational gap rather than a convergence shortfall, and our most
immediate diagnostic target. Two benchmarks, \texttt{or1200}
and \texttt{boundtop}, account for almost all of the seed-to-seed
instability: their per-seed outcomes span $5.8$--$33.5\%$ and
$21.3$--$50.7\%$ respectively, ranges three to four times wider than any
other benchmark in the pool (all $\le8$ points). We analyze this
asymmetry in Section~\ref{sec:results-tradeoff}.

\subsection{GA Head-to-Head Comparisons}
\label{sec:results-ga}

The central claim of this paper is not that the policy finds a
\emph{better} layout than evolutionary search, but that it reaches a
comparable one at a tiny fraction of the search cost. To make that precise,
without the metric- and baseline-matching caveat that attaches to the GOLDS
comparison (Section~\ref{sec:results-golds}), we implemented a genetic
algorithm under the same evaluation oracle, ADP metric, and baseline as the RL
pipeline, and ran it on \texttt{softmax} (held out of RL training;
Table~\ref{tab:benchmarks}): roulette-wheel selection, single-point crossover,
and per-gene mutation over both hard-block coordinates and fabric aspect ratio.
We give the GA a population of \textbf{8}, matching the eight parallel workers
used for the RL runs, capped at 500 generations with early-stop patience of 50
generations without improvement. The RL comparison point is the 11-benchmark
policy (Section~\ref{sec:results-zeroshot}) applied to \texttt{softmax} both
zero-shot and after fine-tuning.

Table~\ref{tab:ga} reports the outcome. The GA converges to
\textbf{\GAred\%} ADP reduction --- essentially the same destination as the
fine-tuned policy's \textbf{14.82\%} --- but only after \textbf{\GAcalls}~VTR
oracle calls spanning \textbf{\GAhours} of wall-clock on a 24-core machine.
The policy reaches \textbf{5.39\%} of that reduction in a \emph{single}
zero-shot oracle call ($\sim$134~s), and the full 14.82\% in 1{,}128 calls of
amortized fine-tuning that begin from an already-competent policy rather than a
random population. The comparison is deliberately stacked in the GA's favor ---
it gets unbounded search time and the same oracle --- and it still pays
\GAcalls\ VTR calls to arrive at the same place the policy reaches in one. Because
that cost is re-incurred from scratch for every new protected IP block, whereas
the policy's cost is paid once and amortized, the practical gap widens with
every additional benchmark a designer must handle.

\begin{table}[t]
\caption{Same destination, very different search cost, on \texttt{softmax}
(baseline ADP $=3.58\!\times\!10^6$; held out of RL training). GA: population
8, $\le$500 generations, early-stop patience 50, run to convergence. RL
zero-shot is a single deterministic rollout (seed 42); RL fine-tuned continues
the 11-benchmark policy on \texttt{softmax}. Wall-clock is on the same 24-core
machine; the GA and the RL fine-tune use the same eight parallel workers.}
\label{tab:ga}
\centering
\begin{tabular}{@{}lrrr@{}}
\toprule
Method & Reduction & VTR calls & Wall-clock \\
\midrule
GA            & \GAred\%        & \GAcalls       & \GAhours \\
RL zero-shot  & 5.39\%          & \textbf{1}     & \textbf{$\sim$2\,min} \\
RL fine-tuned & \textbf{14.82\%}& 1{,}128        & $\sim$8\,h \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:layout} shows where the saving comes from physically on
\texttt{softmax}. The traditional fabric provisions BRAM columns the netlist
never uses and spends area on a generic, over-sized array. The policy's
architecture omits the unused BRAM resource entirely and selects an aspect
ratio under which VTR auto-sizes the same logic onto a $22\%$ smaller array ---
the dominant term in the ADP reduction. The policy never moves a CLB; it
removes idle resource columns and reshapes the fabric, and the tighter routing
that follows is VTR's.

\begin{figure*}[t]
\centering
\includegraphics[width=0.92\textwidth]{fig_layout.pdf}
\caption{What the policy recovers, physically (\texttt{softmax}, the GA
comparison benchmark). Both fabrics are drawn at the \emph{same tile scale}.
The traditional island-style baseline (left) is a $43\times43$ heterogeneous
fabric that provisions BRAM columns (plum) the netlist never uses, with DSP
blocks (terracotta) spread across regular DSP columns. The RL-optimized fabric
(right) tailors the architecture to the netlist: it drops the unused BRAM
columns entirely and repositions the DSPs, under an aspect ratio that lets VTR
auto-size the same logic down to $36\times40$.
The dashed outline is the traditional footprint and the hatched strip is the
reclaimed area --- a $22\%$ smaller fabric, the dominant term in the $15\%$ ADP
reduction. Gaps between tiles are routing channels.}
\label{fig:layout}
\end{figure*}

\subsection{Head-to-Head Against GOLDS}
\label{sec:results-golds}

As a secondary, literature-facing comparison, we recompute a genuine
zero-shot result — a policy trained only on
\texttt{diffeq2}, evaluated zero-shot on \texttt{diffeq1} with no training on
it whatsoever — on GOLDS'~\cite{golds} own 2-term Area$\times$Delay metric
(rather than our 3-term Area$\times$Delay$\times$Power metric used
elsewhere), under the same traditional baseline architecture GOLDS uses. This
yields a \textbf{37.4\%} reduction versus GOLDS' published best-of-population
result of \textbf{$\sim$35\%} on the same benchmark — a real, if narrower,
margin, achieved with zero benchmark-specific training. We report this
result separately from Section~\ref{sec:results-ga} precisely because it
required recomputing on a different metric than the rest of this paper;
nowhere else do we compare 2-term GA numbers against our 3-term RL numbers
directly.

\subsection{Zero-Shot Generalization}
\label{sec:results-zeroshot}

Figure~\ref{fig:seedvar} (lower group) reports ADP reduction on all six
held-out benchmarks (Table~\ref{tab:benchmarks}), across the same three seeds
as Section~\ref{sec:results-inpool}, evaluated with zero benchmark-specific
training. Two of the six (\texttt{softmax}, \texttt{reduction\_layer}) were
part of the training-time \emph{universe} used to size the policy's tensors
(Section~\ref{sec:universe}) but never sampled during training; the other
four were entirely outside the universe as well, a strictly harder
generalization test. Crucially, four of the six are fabrics
\emph{larger than anything in the training pool} (up to $2{,}304$ tiles versus
the pool's $676$; Figure~\ref{fig:scale}), so this is size extrapolation, not
interpolation. Every benchmark finishes positive under every seed —
no sign flips at all, unlike the in-pool result — and every per-seed range is
narrow (at most $\approx12$ points, for \texttt{custom\_macbuf}), with no
benchmark approaching the \texttt{or1200}/\texttt{boundtop} spread.
The overall average across all six benchmarks and three seeds is
\textbf{31.23\%}. The strongest, most reliable benchmarks
(\texttt{mkDelayWorker32B}, \texttt{custom\_macbuf}) are also the most
seed-stable in relative terms (coefficient of variation 5--10\%), while the
weakest (\texttt{softmax}, \texttt{arm\_core}) are proportionally the least
certain (coefficient of variation 22--34\%) — the absolute spread is small
everywhere, but on a small mean that spread represents a much larger fraction
of the result.

\textbf{A note on deterministic decoding.} Every number above uses greedy
decoding (the policy's most-likely action at each step), which is the intended
deployment mode: a layout is generated once and baked into the architecture,
not sampled. As a diagnostic, we drew 10 \emph{stochastic} rollouts per
benchmark on one seed; their average ADP reduction was negative for 4 of 6
benchmarks despite every deterministic result being positive. This gap is
expected and does not invalidate the deterministic numbers — it shows the
learned distribution is concentrated, with most mass away from the
high-quality region the top choice occupies. The zero-shot results should
thus be read as the top choice's performance under each seed, not an
expectation over sampling; a full three-seed stochastic evaluation is left to
future work.

\subsection{Fine-Tuning Efficiency}
\label{sec:results-finetune}

We also characterize continued learning on a partially-known benchmark
(\texttt{diffeq1}, in the training pool): fine-tuning the multi-benchmark
policy versus training from scratch, both 6{,}000 episodes under identical
hyperparameters and separate caches. The fine-tuned run dominates typical
reward throughout (0.785 vs.\ 0.736 in the final window) and plateaus by
$\approx$episode 3{,}000, while scratch never plateaus within its budget. The
mechanism is entropy collapse: the fine-tuned policy's entropy falls from 2.00
to 0.79 (vs.\ scratch's 4.63 to 2.42), so only 31.5\% of its episodes are
novel VTR evaluations versus 94.4\% for scratch — a sample-efficiency win
that, as Section~\ref{sec:results-tradeoff} notes, also caps further
exploration beyond the pretrained prior. This is continued learning, not
cross-benchmark transfer (Section~\ref{sec:results-zeroshot}).

\subsection{Seed Variance: In-Pool vs.\ Zero-Shot}
\label{sec:results-tradeoff}

Across three independent training seeds, the 11-benchmark policy exhibits a
clear asymmetry: \emph{in-pool} performance is considerably more
seed-sensitive than \emph{zero-shot} performance. For nine of the eleven
training benchmarks, the per-seed range is small ($\le8$ points); two
benchmarks, \texttt{or1200} and \texttt{boundtop}, span $\sim$28 and
$\sim$29 points respectively — three to four times the spread of any other
benchmark — and account for almost the entire spread in the pooled aggregate
(35.73\% / 23.11\% / 23.97\% across seeds). On the six held-out zero-shot
benchmarks, by contrast, every per-seed range stays within $\sim$12 points
(Figure~\ref{fig:seedvar}); the two shaded in-pool benchmarks have the widest
ranges in either group.

We do not have a confirmed mechanism but propose one. In-pool performance
depends on which benchmarks a given seed converges on slowly within the shared
episode budget, whereas zero-shot performance reflects a more general property
of the learned representation (Section~\ref{sec:gnn}), independent of any one
benchmark's convergence. The entropy collapse of
Section~\ref{sec:results-finetune} compounds this: a seed converging early on
most benchmarks retains fewer episodes for a slow one like \texttt{or1200}.
No seed leads on both volatile benchmarks and elsewhere at once
(seed 42 leads on \texttt{or1200} and \texttt{boundtop}; seeds 7 and 123 lead
on others), pointing to per-benchmark convergence variance rather than a
globally better seed. At $n=3$ this is a characterization; more seeds would
establish whether the asymmetry is stable.

\section{Conclusion}
\label{sec:conclusion}

We asked whether a learned policy can replace per-instance evolutionary
search for custom eFPGA-based logic masking. Under a controlled comparison
on an identical benchmark, oracle, ADP metric, and baseline, the RL policy
reaches the same ADP reduction as our genetic algorithm while paying a tiny
fraction of its search cost: a single zero-shot rollout lands in the GA's
region at one oracle call, against the \GAcalls\ calls and \GAhours\ of search
the GA re-pays from scratch for every new netlist (Section~\ref{sec:results-ga}).
The same policy generalizes zero-shot to six held-out benchmarks --- four of
them fabrics larger than anything seen in training (Figure~\ref{fig:scale}) ---
and, recomputed on GOLDS' published metric, exceeds GOLDS' reported result
(Sections~\ref{sec:results-golds},~\ref{sec:results-zeroshot}).

Two reliability findings qualify these results and matter for reproduction.
In-pool fitting is far more seed-sensitive than zero-shot transfer (the
aggregate ranges 23.11--35.73\% across seeds, driven by \texttt{or1200} and
\texttt{boundtop}), and the deterministic zero-shot numbers diverge from
stochastic sampling (negative for 4 of 6 benchmarks in a preliminary check).
A single-seed or single-rollout number is therefore not a stable
characterization of the policy (Section~\ref{sec:results-tradeoff}).

Several gaps remain. Results rest on three seeds, and a systematic
stochastic-rollout evaluation across all seeds is still outstanding. The
action space covers DSP/BRAM placement and aspect ratio only; the NSGA-II
follow-up~\cite{nsgaii} additionally co-evolves CLB LUT size and
arithmetic-block sizing, a natural extension given that the GCN representation
(Section~\ref{sec:gnn}) already generalizes across netlist size and resource
mix. Finally, broadening the benchmark pool to the full GOLDS/NSGA-II set
(e.g.\ \texttt{sha}, the Koios suite) would let every comparison be made
directly rather than through the metric conversion of
Section~\ref{sec:results-golds}.

\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}
